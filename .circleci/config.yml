# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: codelovers/nodejs-aws-cli

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mongo:3.4.4

    working_directory: ~/apaettie_backup

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run: npm install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

#      - run: npm test
      - run: npm run build

      - deploy:
           name: Deploy to S3 (test site if not master branch)
           command: |
             if [ "${CIRCLE_BRANCH}" == "master" ]; then
               echo "=====  Deploying MASTER to PROD  ====="
               mkdir build/projects
               mv build/projects.html build/projects/index.html
               mv build/resume.html build/resume/index.html
               aws s3 sync build s3://andrewpaettie.com --delete --acl public-read
             else
               echo "=====  Deploying ${CIRCLE_BRANCH} to TEST  ====="
               mkdir build/projects
               mv build/projects.html build/projects/index.html
               mv build/resume.html build/resume/index.html
               aws s3 sync build s3://andrewpaettie.test.com --delete --acl public-read
             fi